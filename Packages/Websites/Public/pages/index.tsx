import React from "react";
import styled from "@emotion/styled";

const StyledPage = styled.div``;

export const Index = () => {
  return <StyledPage>This is a NEXT.JS WEBSITE</StyledPage>;
};

export default Index;
